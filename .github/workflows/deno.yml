name: Node.js Package

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout the full repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # ensures the full history is available

      # 2️⃣ Run Hashpin Enforcer AFTER checkout
      - name: Run Hashpin Enforcer
        uses: gustavtjac/HashpinEnforcer@551d437789d0a0bfbb6aaf937a6fd1bc5ef82f55
